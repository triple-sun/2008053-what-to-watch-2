export enum Config {
  PortDoc = 'Port for incoming connections',
  PortEnv = 'DOC',
  PortFormat = 'ipaddress',
  SaltDoc = 'Salt for password hash',
  SaltEnv = 'SALT',
  DBDoc = 'IP address of the database server (MongoDB)',
  DBEnv = 'DB_HOST',
  DBFormat = 'ipaddress'
}
