export enum Env {
  Port = 'PORT',
  Salt = 'SALT',
  Upload = 'UPLOAD',
  JWTSecret = 'JWT_SECRET',
  DBHost = 'DB_HOST',
  DBPort = 'DB_PORT',
  DBUser = 'DB_USER',
  DBPass = 'DB_PASS',
  DBName = 'DB_NAME',
}
